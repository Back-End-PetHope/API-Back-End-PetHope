name: Build

on:
  push:
    branches:
      - "main"
      - "dev_main*"
  pull_request:
    branches:
      - "main"

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Hello World
        run: echo "Hello World!"